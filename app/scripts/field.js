// field.js
// フィールドの情報すべて
// スコア、グリッド等

let grid = [];

for(let i = 0;i < boardSize; i++) {	//ボードのマス数だけ配列を増やしすべてに0を入れる
	grid.push(0);
}

let gridB = [];

for(let i = 0;i < boardSize; i++) {	//ボードのマス数だけ配列を増やしすべてに0を入れる
	gridB.push(0);
}
